{
  "entities": {
    "OverlayData": {
      "title": "Overlay Data",
      "description": "Real-time translation data for OBS overlay",
      "type": "object",
      "properties": {
        "text": {
          "type": "string",
          "description": "The current translation text"
        },
        "updatedAt": {
          "type": "number",
          "description": "Timestamp of the last update"
        }
      },
      "required": ["text", "updatedAt"]
    }
  },
  "firestore": {
    "/overlay_data/current_translation": {
      "schema": "OverlayData",
      "description": "The single document used to sync translation to OBS"
    }
  }
}
